---
export interface Review {
  text: string;
  name: string;
  date: Date;
  source?: string;
}

export interface Props {
  reviews: Review[];
}

const { reviews } = Astro.props;
const tickerReviews = [...reviews, ...reviews];
// ~8 seconds per review for a calm pace
const tickerDuration = reviews.length * 8;
---

<div class="w-full pointer-events-none" dir="rtl">
<div class="review-ticker-container w-full overflow-hidden py-1">
  <div class="ticker-track flex gap-3" style={`animation-duration: ${tickerDuration}s;`}>
    {tickerReviews.map((review) => (
      <div class="review-card bg-white/80 dark:bg-slate-800/80 backdrop-blur-sm rounded-lg px-3 py-2 shadow-sm flex-shrink-0">
        <p class="text-xs text-gray-700 dark:text-gray-300 whitespace-nowrap">"{review.text}"</p>
        <p class="text-[9px] text-muted whitespace-nowrap text-left mt-1">— {review.name}، {review.date.toLocaleDateString('ar-JO')}{review.source ? ` (${review.source})` : ''}</p>
      </div>
    ))}
  </div>
</div>
<p class="text-[10px] sm:text-[9px] text-muted/60 ml-2 mt-1 text-left pointer-events-none">هذه كلمات المراجعين كما خطوها بأنفسهم<span class="sm:hidden"><br/></span><span class="hidden sm:inline"> </span>في مواقع طبكان وغوغل، وعلى الخاص</p>
</div>

<style>
  .ticker-track {
    animation: tickerScroll 35s linear infinite;
    width: max-content;
  }

  @keyframes tickerScroll {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(50%);
    }
  }

  .review-ticker-container {
    mask-image: linear-gradient(to left, transparent 0%, black 15%, black 85%, transparent 100%);
    -webkit-mask-image: linear-gradient(to left, transparent 0%, black 15%, black 85%, transparent 100%);
  }
</style>
