---
import Layout from '~/layouts/Layout.astro';
import { getHomePermalink } from '~/utils/permalinks';

const title = `استشارة مرئية عن بعد`;
---

<Layout metadata={{ title }}>
  <section class="flex items-center h-full p-16">
    <div class="container flex flex-col items-center justify-center px-5 mx-auto my-8">
      <div class="max-w-md text-center">
        <h2 class="mb-8 font-bold text-6xl md:text-7xl">
          <span class="text-primary">استشارة عن بعد</span>
        </h2>
        <div class="mb-6 p-4 bg-yellow-100 dark:bg-yellow-900/30 rounded-lg border border-yellow-300 dark:border-yellow-700">
          <p class="text-xl font-semibold text-yellow-800 dark:text-yellow-200">
            هذه الميزة قيد التطوير
          </p>
        </div>
        <p class="mt-4 mb-8 text-lg text-muted dark:text-slate-400">
          نعمل على إتاحة هذه الخدمة قريباً. شكراً لصبركم.
        </p>
        <a rel="noopener noreferrer" href={getHomePermalink()} class="btn">العودة للصفحة الرئيسية</a>
      </div>
    </div>
  </section>
</Layout>
